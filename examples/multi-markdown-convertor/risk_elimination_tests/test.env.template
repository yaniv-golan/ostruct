# Template for test environment variables
# Copy this to .env and fill in your values

# OpenAI API Key (required for LLM tests)
OPENAI_API_KEY=your_openai_api_key_here

# Ostruct configuration
OSTRUCT_DISABLE_REGISTRY_UPDATE_CHECKS=true

# Note: Token limits and model selection are controlled via ostruct CLI flags:
# ostruct run template.j2 schema.json -m gpt-4.1 --max-tokens 16000
#
# Common model options:
# -m gpt-4.1          # Default model (good balance of cost/performance)
# -m gpt-4o           # Previous generation
# -m o3               # Premium model (most expensive)