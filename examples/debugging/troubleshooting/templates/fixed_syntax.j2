This template shows the CORRECTED versions of common syntax errors.

# Fixed Syntax Examples

## Fix 1: Properly closed if statement
{% if user_name %}
Hello {{ user_name }}!
{% endif %}

## Fix 2: Matching tags
{% for item in items %}
- {{ item }}
{% endfor %}

## Fix 3: Safe variable access with default
Hello {{ user.get('name', {}).get('first', 'Unknown') }}!

## Fix 4: Properly closed expression
{{ "Hello " + (user_name or "World") }}

## Fix 5: Valid Jinja2 filter syntax
{{ items | select('>', 5) | list }}

## Fix 6: Properly nested template tags
{% if condition %}
  {% if another_condition %}
    Content here
  {% endif %}
{% endif %}

## Fix 7: Proper quotation handling
{{ 'Hello "world"' }}
{# or #}
{{ "Hello 'world'" }}

{
  "troubleshooting_info": {
    "syntax_valid": true,
    "errors_fixed": 7,
    "best_practices": [
      "Always close template tags",
      "Use matching tag pairs",
      "Handle missing variables gracefully",
      "Use proper Jinja2 filter syntax",
      "Nest tags correctly",
      "Handle quotes properly"
    ]
  }
}
